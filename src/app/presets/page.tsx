import { PresetBrowse } from "@/components/presets/PresetBrowse";
export const metadata = { title: "Presets · Watchface Studio" };
export default function PresetsPage() {
  return <PresetBrowse />;
}
